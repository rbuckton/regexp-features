{{#each names as |name| }}
{{> sources sources=(lookup .. (concat name 'Sources')) name=name }}
{{else if sources}}

<!--
{{#if name}}'{{name}}' {{/if}}sources:
{{#each sources}}
  - []({{ relative @root.outFile . }})
{{/each}}
-->

{{/each}}